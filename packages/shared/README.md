# @autobiz/shared

Locked types + zod schemas for every track. Every other package imports from here.

**Do not modify these files** except via the change protocol in [`CONTRACTS.md §9`](../../CONTRACTS.md).

Exports: types + `<Type>Schema` zod validators for every entity in `CONTRACTS.md §1`.
